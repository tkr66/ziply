packs:
  test:
    filename: test.zip
    entries:
      - src: ./src/main.rs
        dest: /src/main.rs
      - src: ./Cargo.lock
        dest: /Cargo.lock
      - src: ./Cargo.toml
        dest: /Cargo.toml

